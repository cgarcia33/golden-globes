python version: python 3
setup: gg.json file(s) must be placed in the top level of the directory
running the code: "python gg_api.py" will call pre_ceremony() and then main() as defined in gg_api.py, "python3 gg_api.py" on macOS
outputs: main() will output a human readable year+results.txt file and a year+results.json file for each year
repository: https://github.com/cgarcia33/golden-globes
